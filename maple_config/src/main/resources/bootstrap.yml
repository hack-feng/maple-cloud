spring:
  application:
    name: maple_config
  cloud:
    config:
      profile: dev
      server:
        git:
          uri: https://github.com/hack-feng/maple-properties
          search-paths: /
      label: master
